:-assert(robot_activity_experience(
  bin_1_1_10,    %% episode ID
  bin,    %% domain
  [
    clean(table1,bin1),    %% task
    [        %% key propositions
        at_start(at(robot1,bin1)),
        at_start(gripper(robot1,carry)),
        at_start(on(bottle1,table1)),
        at_start(on(bottle2,table1)),
        at_start(on(bottle3,table1)),
        at_start(on(bottle4,table1)),
        at_start(on(bottle5,table1)),
        at_start(on(bottle6,table1)),
        at_start(on(bottle7,table1)),
        at_start(on(bottle8,table1)),
        at_start(on(bottle9,table1)),
        at_start(on(bottle10,table1)),
        throughout(table(table1)),
        throughout(bin(bin1)),
        throughout(posture(open)),
        throughout(posture(close)),
        throughout(posture(carry)),
        throughout(reach(robot1,table1)),
        throughout(reach(robot1,bin1)),
        at_end(in(bottle1,bin1)),
        at_end(in(bottle2,bin1)),
        at_end(in(bottle3,bin1)),
        at_end(in(bottle4,bin1)),
        at_end(in(bottle5,bin1)),
        at_end(in(bottle6,bin1)),
        at_end(in(bottle7,bin1)),
        at_end(in(bottle8,bin1)),
        at_end(in(bottle9,bin1)),
        at_end(in(bottle10,bin1))
    ],
    none,               %% failed operation
    race_dummy,         %% failure type
    [
        grip(robot1,carry,open),
        move(robot1,bin1,table1),
        pick(robot1,bottle1,table1),
        move(robot1,table1,bin1),
        drop(robot1,bottle1,bin1),
        move(robot1,bin1,table1),
        pick(robot1,bottle2,table1),
        move(robot1,table1,bin1),
        drop(robot1,bottle2,bin1),
        move(robot1,bin1,table1),
        pick(robot1,bottle3,table1),
        move(robot1,table1,bin1),
        drop(robot1,bottle3,bin1),
        move(robot1,bin1,table1),
        pick(robot1,bottle4,table1),
        move(robot1,table1,bin1),
        drop(robot1,bottle4,bin1),
        move(robot1,bin1,table1),
        pick(robot1,bottle5,table1),
        move(robot1,table1,bin1),
        drop(robot1,bottle5,bin1),
        move(robot1,bin1,table1),
        pick(robot1,bottle6,table1),
        move(robot1,table1,bin1),
        drop(robot1,bottle6,bin1),
        move(robot1,bin1,table1),
        pick(robot1,bottle7,table1),
        move(robot1,table1,bin1),
        drop(robot1,bottle7,bin1),
        move(robot1,bin1,table1),
        pick(robot1,bottle8,table1),
        move(robot1,table1,bin1),
        drop(robot1,bottle8,bin1),
        move(robot1,bin1,table1),
        pick(robot1,bottle9,table1),
        move(robot1,table1,bin1),
        drop(robot1,bottle9,bin1),
        move(robot1,bin1,table1),
        pick(robot1,bottle10,table1),
        move(robot1,table1,bin1),
        drop(robot1,bottle10,bin1)
    ]
  ]
)).
