:-assert(plan(
    %%% clean table1 bin1
   [grip(arm1, carry, open),
    pick(arm1, bottle1, table1),
    move(arm1, table1, bin1),
    grip(arm1, hold, open),
    move(arm1, bin1, table1),
    pick(arm1, bottle2, table1),
    move(arm1, table1, bin1),
    drop(arm1, bottle1, bin1),
    move(arm1, bin1, table1),
    pick(arm1, bottle3, table1),
    move(arm1, table1, bin1),
    drop(arm1, bottle2, bin1),
    move(arm1, bin1, table1),
    pick(arm1, bottle4, table1),
    move(arm1, table1, bin1),
    drop(arm1, bottle3, bin1),
    move(arm1, bin1, table1),
    pick(arm1, bottle5, table1),
    move(arm1, table1, bin1),
    drop(arm1, bottle4, bin1),
    move(arm1, bin1, table1),
    pick(arm1, bottle6, table1),
    move(arm1, table1, bin1),
    drop(arm1, bottle5, bin1),
    move(arm1, bin1, table1),
    pick(arm1, bottle7, table1),
    move(arm1, table1, bin1),
    drop(arm1, bottle6, bin1),
    move(arm1, bin1, table1),
    pick(arm1, bottle8, table1),
    move(arm1, table1, bin1),
    drop(arm1, bottle7, bin1),
    move(arm1, bin1, table1),
    pick(arm1, bottle9, table1),
    move(arm1, table1, bin1),
    drop(arm1, bottle8, bin1),
    move(arm1, bin1, table1),
    pick(arm1, bottle10, table1),
    move(arm1, table1, bin1),
    drop(arm1, bottle9, bin1),
    grip(arm1, open, hold),
    drop(arm1, bottle10, bin1)])).